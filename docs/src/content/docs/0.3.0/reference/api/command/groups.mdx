---
title: Command::groups
description: The `Command::groups` API.
sidebar:
  label: groups
slug: 0.3.0/reference/api/command/groups
---

import { Tabs, TabItem } from '@astrojs/starlight/components';
import { Badge } from '@astrojs/starlight/components';
import { Icon } from '@astrojs/starlight/components';
import { LinkButton } from '@astrojs/starlight/components';

# `groups` <LinkButton href="https://docs.rs/clap/latest/clap/struct.Command.html#method.groups" icon="open-book" variant="minimal" />

<Badge text="since 0.1.0" size="small" />

Adds multiple `ArgGroups` to the Command at once.

# Example

### Spec

<Tabs syncKey="format">
  <TabItem label="TOML">
    ```toml
    name = "cmd"
    [groups]
    vers = { args = ["set-ver", "major", "minor", "patch"], required = true }
    input = { args = ["config", "iface"] }
    [args]
    set-ver = { long = "set-ver", help = "set the version manually", required = false }
    major = { long = "major", help = "auto increase major" }
    minor = { long = "minor", help = "auto increase minor" }
    patch = { long = "patch", help = "auto increase patch" }
    config = { short = 'c', help = "a config file", required = false }
    iface = { short = 'i', help = "an interface", required = false }
    ```
  </TabItem>

  <TabItem label="YAML">
    ```yaml
    name: cmd
    groups:
      vers:
        args:
        - set-ver
        - major
        - minor
        - patch
        required: true
      input:
        args:
        - config
        - iface
    args:
      set-ver:
        long: set-ver
        help: set the version manually
        required: false
      major:
        long: major
        help: auto increase major
      minor:
        long: minor
        help: auto increase minor
      patch:
        long: patch
        help: auto increase patch
      config:
        short: c
        help: a config file
        required: false
      iface:
        short: i
        help: an interface
        required: false
    ```
  </TabItem>

  <TabItem label="JSON">
    ```json
    {
      "name": "cmd",
      "groups": {
        "vers": {
          "args": [
            "set-ver",
            "major",
            "minor",
            "patch"
          ],
          "required": true
        },
        "input": {
          "args": [
            "config",
            "iface"
          ]
        }
      },
      "args": {
        "set-ver": {
          "long": "set-ver",
          "help": "set the version manually",
          "required": false
        },
        "major": {
          "long": "major",
          "help": "auto increase major"
        },
        "minor": {
          "long": "minor",
          "help": "auto increase minor"
        },
        "patch": {
          "long": "patch",
          "help": "auto increase patch"
        },
        "config": {
          "short": "c",
          "help": "a config file",
          "required": false
        },
        "iface": {
          "short": "i",
          "help": "an interface",
          "required": false
        }
      }
    }
    ```
  </TabItem>
</Tabs>

### Input

```bash
cmd --major 1 -i eth0
```

### Output

```bash
claptrap_major='1'
claptrap_iface='eth0'
```
