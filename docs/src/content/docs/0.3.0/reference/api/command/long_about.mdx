---
title: Command::long_about
description: The `Command::long_about` API.
sidebar:
  label: long_about
slug: 0.3.0/reference/api/command/long_about
---

import { Tabs, TabItem } from '@astrojs/starlight/components';
import { Badge } from '@astrojs/starlight/components';
import { Icon } from '@astrojs/starlight/components';
import { LinkButton } from '@astrojs/starlight/components';

# `long_about` <LinkButton href="https://docs.rs/clap/latest/clap/struct.Command.html#method.long_about" icon="open-book" variant="minimal" />

<Badge text="since 0.1.0" size="small" />

Sets the program’s description for the long help (`--help`).

# Example

### Spec

<Tabs syncKey="format">
  <TabItem label="TOML">
    ```toml
    name = "myprog"
    long-about = "Does really amazing things to great people. Now let's talk a little more in depth about how this subcommand really works. It may take about a few lines of text, but that's ok!"
    [args]
    ```
  </TabItem>

  <TabItem label="YAML">
    ```yaml
    name: myprog
    long-about: Does really amazing things to great people. Now let's talk a little more in depth about how this subcommand really works. It may take about a few lines of text, but that's ok!
    args: {}
    ```
  </TabItem>

  <TabItem label="JSON">
    ```json
    {
      "name": "myprog",
      "long-about": "Does really amazing things to great people. Now let's talk a little more in depth about how this subcommand really works. It may take about a few lines of text, but that's ok!",
      "args": {}
    }
    ```
  </TabItem>
</Tabs>

### Input

```bash
myprog --help
```

### Output

```bash
Does really amazing things to great people. Now let's talk a little more in depth about how this subcommand really works. It may take about a few lines of text,
but that's ok!

Usage: myprog

Options:
  -h, --help
          Print help (see a summary with '-h')
```
