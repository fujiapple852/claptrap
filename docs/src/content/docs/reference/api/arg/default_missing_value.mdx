---
title: Arg::default_missing_value
description: The `Arg::default_missing_value` API.
banner:
  content: 🚧 🚧 🚧 Under construction 🚧 🚧 🚧
sidebar:
  label: default_missing_value
---

import { Tabs, TabItem } from '@astrojs/starlight/components';
import { Badge } from '@astrojs/starlight/components';
import { Icon } from '@astrojs/starlight/components';
import { LinkButton } from '@astrojs/starlight/components';

# `default_missing_value` <LinkButton href="https://docs.rs/clap/latest/clap/struct.Arg.html#method.default_missing_value" icon="open-book" variant="minimal"></LinkButton>

<Badge text="since 0.1.0" size="small" />

Value for the argument when the flag is present but no value is specified.

# Examples
## Example 1

### Spec

<Tabs syncKey="format">
  <TabItem label="TOML">
```toml
name = "prog"
[args]
color = { long = "color", value-name = "COLOR", value-parser = ["always", "auto", "never"], default-value = "auto", num-args = "optional", require-equals = true, default-missing-value = "always", help = "Specify WHEN to colorize output." }
```
  </TabItem>
  <TabItem label="YAML">
```yaml
name: prog
args:
  color:
    long: color
    value-name: COLOR
    value-parser:
    - always
    - auto
    - never
    default-value: auto
    num-args: optional
    require-equals: true
    default-missing-value: always
    help: Specify WHEN to colorize output.
```
  </TabItem>
  <TabItem label="JSON">
```json
{
  "name": "prog",
  "args": {
    "color": {
      "long": "color",
      "value-name": "COLOR",
      "value-parser": [
        "always",
        "auto",
        "never"
      ],
      "default-value": "auto",
      "num-args": "optional",
      "require-equals": true,
      "default-missing-value": "always",
      "help": "Specify WHEN to colorize output."
    }
  }
}
```
  </TabItem>
</Tabs>

### Input

```bash
prog
```

### Output

```bash
claptrap_color='auto'
```
## Example 2

### Spec

<Tabs syncKey="format">
  <TabItem label="TOML">
```toml
name = "prog"
[args]
color = { long = "color", value-name = "COLOR", value-parser = ["always", "auto", "never"], default-value = "auto", num-args = "optional", require-equals = true, default-missing-value = "always", help = "Specify WHEN to colorize output." }
```
  </TabItem>
  <TabItem label="YAML">
```yaml
name: prog
args:
  color:
    long: color
    value-name: COLOR
    value-parser:
    - always
    - auto
    - never
    default-value: auto
    num-args: optional
    require-equals: true
    default-missing-value: always
    help: Specify WHEN to colorize output.
```
  </TabItem>
  <TabItem label="JSON">
```json
{
  "name": "prog",
  "args": {
    "color": {
      "long": "color",
      "value-name": "COLOR",
      "value-parser": [
        "always",
        "auto",
        "never"
      ],
      "default-value": "auto",
      "num-args": "optional",
      "require-equals": true,
      "default-missing-value": "always",
      "help": "Specify WHEN to colorize output."
    }
  }
}
```
  </TabItem>
</Tabs>

### Input

```bash
prog --color=never
```

### Output

```bash
claptrap_color='never'
```
## Example 3

### Spec

<Tabs syncKey="format">
  <TabItem label="TOML">
```toml
name = "prog"
[args]
color = { long = "color", value-name = "COLOR", value-parser = ["always", "auto", "never"], default-value = "auto", num-args = "optional", require-equals = true, default-missing-value = "always", help = "Specify WHEN to colorize output." }
```
  </TabItem>
  <TabItem label="YAML">
```yaml
name: prog
args:
  color:
    long: color
    value-name: COLOR
    value-parser:
    - always
    - auto
    - never
    default-value: auto
    num-args: optional
    require-equals: true
    default-missing-value: always
    help: Specify WHEN to colorize output.
```
  </TabItem>
  <TabItem label="JSON">
```json
{
  "name": "prog",
  "args": {
    "color": {
      "long": "color",
      "value-name": "COLOR",
      "value-parser": [
        "always",
        "auto",
        "never"
      ],
      "default-value": "auto",
      "num-args": "optional",
      "require-equals": true,
      "default-missing-value": "always",
      "help": "Specify WHEN to colorize output."
    }
  }
}
```
  </TabItem>
</Tabs>

### Input

```bash
prog --color
```

### Output

```bash
claptrap_color='always'
```
## Example 4

### Spec

<Tabs syncKey="format">
  <TabItem label="TOML">
```toml
name = "prog"
[args]
create = { long = "create", value-name = "BOOL", value-parser = ":bool:", num-args = "optional", require-equals = true, default-missing-value = "true" }
```
  </TabItem>
  <TabItem label="YAML">
```yaml
name: prog
args:
  create:
    long: create
    value-name: BOOL
    value-parser: ':bool:'
    num-args: optional
    require-equals: true
    default-missing-value: 'true'
```
  </TabItem>
  <TabItem label="JSON">
```json
{
  "name": "prog",
  "args": {
    "create": {
      "long": "create",
      "value-name": "BOOL",
      "value-parser": ":bool:",
      "num-args": "optional",
      "require-equals": true,
      "default-missing-value": "true"
    }
  }
}
```
  </TabItem>
</Tabs>

### Input

```bash
prog
```

### Output

```bash

```
## Example 5

### Spec

<Tabs syncKey="format">
  <TabItem label="TOML">
```toml
name = "prog"
[args]
create = { long = "create", value-name = "BOOL", value-parser = ":bool:", num-args = "optional", require-equals = true, default-missing-value = "true" }
```
  </TabItem>
  <TabItem label="YAML">
```yaml
name: prog
args:
  create:
    long: create
    value-name: BOOL
    value-parser: ':bool:'
    num-args: optional
    require-equals: true
    default-missing-value: 'true'
```
  </TabItem>
  <TabItem label="JSON">
```json
{
  "name": "prog",
  "args": {
    "create": {
      "long": "create",
      "value-name": "BOOL",
      "value-parser": ":bool:",
      "num-args": "optional",
      "require-equals": true,
      "default-missing-value": "true"
    }
  }
}
```
  </TabItem>
</Tabs>

### Input

```bash
prog --create=false
```

### Output

```bash
claptrap_create='false'
```
## Example 6

### Spec

<Tabs syncKey="format">
  <TabItem label="TOML">
```toml
name = "prog"
[args]
create = { long = "create", value-name = "BOOL", value-parser = ":bool:", num-args = "optional", require-equals = true, default-missing-value = "true" }
```
  </TabItem>
  <TabItem label="YAML">
```yaml
name: prog
args:
  create:
    long: create
    value-name: BOOL
    value-parser: ':bool:'
    num-args: optional
    require-equals: true
    default-missing-value: 'true'
```
  </TabItem>
  <TabItem label="JSON">
```json
{
  "name": "prog",
  "args": {
    "create": {
      "long": "create",
      "value-name": "BOOL",
      "value-parser": ":bool:",
      "num-args": "optional",
      "require-equals": true,
      "default-missing-value": "true"
    }
  }
}
```
  </TabItem>
</Tabs>

### Input

```bash
prog --create
```

### Output

```bash
claptrap_create='true'
```
