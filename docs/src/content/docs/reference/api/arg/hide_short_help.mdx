---
title: Arg::hide_short_help
description: The `Arg::hide_short_help` API.
banner:
  content: 🚧 🚧 🚧 Under construction 🚧 🚧 🚧
sidebar:
  label: hide_short_help
---

import { Tabs, TabItem } from '@astrojs/starlight/components';
import { Badge } from '@astrojs/starlight/components';
import { Icon } from '@astrojs/starlight/components';
import { LinkButton } from '@astrojs/starlight/components';

# `hide_short_help` <LinkButton href="https://docs.rs/clap/latest/clap/struct.Arg.html#method.hide_short_help" icon="open-book" variant="minimal"></LinkButton>

<Badge text="since 0.1.0" size="small" />

Hides an argument from short help (`-h`).

# Examples
## Example 1

### Spec

<Tabs syncKey="format">
  <TabItem label="TOML">
```toml
name = "prog"
[args]
cfg = { long = "config", hide-short-help = true, help = "Some help text describing the --config arg" }
```
  </TabItem>
  <TabItem label="YAML">
```yaml
name: prog
args:
  cfg:
    long: config
    hide-short-help: true
    help: Some help text describing the --config arg
```
  </TabItem>
  <TabItem label="JSON">
```json
{
  "name": "prog",
  "args": {
    "cfg": {
      "long": "config",
      "hide-short-help": true,
      "help": "Some help text describing the --config arg"
    }
  }
}
```
  </TabItem>
</Tabs>

### Input

```bash
prog -h
```

### Output

```bash
Usage: prog [OPTIONS]

Options:
  -h, --help  Print help (see more with '--help')
```
## Example 2

### Spec

<Tabs syncKey="format">
  <TabItem label="TOML">
```toml
name = "prog"
[args]
cfg = { long = "config", hide-short-help = true, help = "Some help text describing the --config arg" }
```
  </TabItem>
  <TabItem label="YAML">
```yaml
name: prog
args:
  cfg:
    long: config
    hide-short-help: true
    help: Some help text describing the --config arg
```
  </TabItem>
  <TabItem label="JSON">
```json
{
  "name": "prog",
  "args": {
    "cfg": {
      "long": "config",
      "hide-short-help": true,
      "help": "Some help text describing the --config arg"
    }
  }
}
```
  </TabItem>
</Tabs>

### Input

```bash
prog --help
```

### Output

```bash
Usage: prog [OPTIONS]

Options:
      --config <cfg>
          Some help text describing the --config arg

  -h, --help
          Print help (see a summary with '-h')
```
