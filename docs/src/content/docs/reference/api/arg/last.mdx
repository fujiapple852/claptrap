---
title: Arg::last
description: The `Arg::last` API.
banner:
  content: 🚧 🚧 🚧 Under construction 🚧 🚧 🚧
sidebar:
  label: last
---

import { Tabs, TabItem } from '@astrojs/starlight/components';
import { Badge } from '@astrojs/starlight/components';
import { Icon } from '@astrojs/starlight/components';
import { LinkButton } from '@astrojs/starlight/components';

# `last` <LinkButton href="https://docs.rs/clap/latest/clap/struct.Arg.html#method.last" icon="open-book" variant="minimal"></LinkButton>

<Badge text="since 0.1.0" size="small" />

This arg is the last, or final, positional argument (i.e. has the highest index) and is only able to be accessed via the `--` syntax (i.e. `$ prog args -- last_arg`).

# Examples
## Example 1

### Spec

<Tabs syncKey="format">
  <TabItem label="TOML">
```toml
name = "prog"
[args]
first = {}
second = {}
third = { action = "set", last = true }
```
  </TabItem>
  <TabItem label="YAML">
```yaml
name: prog
args:
  first: {}
  second: {}
  third:
    action: set
    last: true
```
  </TabItem>
  <TabItem label="JSON">
```json
{
  "name": "prog",
  "args": {
    "first": {},
    "second": {},
    "third": {
      "action": "set",
      "last": true
    }
  }
}
```
  </TabItem>
</Tabs>

### Input

```bash
prog one -- three
```

### Output

```bash
claptrap_first='one'
claptrap_third='three'
```
## Example 2

### Spec

<Tabs syncKey="format">
  <TabItem label="TOML">
```toml
name = "prog"
[args]
first = {}
second = {}
third = { action = "set", last = true }
```
  </TabItem>
  <TabItem label="YAML">
```yaml
name: prog
args:
  first: {}
  second: {}
  third:
    action: set
    last: true
```
  </TabItem>
  <TabItem label="JSON">
```json
{
  "name": "prog",
  "args": {
    "first": {},
    "second": {},
    "third": {
      "action": "set",
      "last": true
    }
  }
}
```
  </TabItem>
</Tabs>

### Input

```bash
prog one two three
```

### Output

```bash
error: unexpected argument 'three' found

Usage: prog [first] [second] [-- <third>]

For more information, try '--help'.
```
