name = "prog"
[args]
color = { long = "color", value-name = "COLOR", value-parser = ["always", "auto", "never"], default-value = "auto", num-args = "0..", require-equals = true, default-missing-values-os = ["always", "auto"], help = "Specify WHEN to colorize output." }
